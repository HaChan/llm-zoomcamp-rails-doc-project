MethodsNnewClass Public methodsnew(current: nil, stored: nil)LinkSource:show|on GitHub# File activerecord/lib/active_record/migration.rb, line 220definitialize(current:nil,stored:nil)msg=+"You are attempting to modify a database that was last run in `#{ stored }` environment.\n"msg<<"You are running in `#{ current }` environment. "msg<<"If you are sure you want to continue, first set the environment using:\n\n"msg<<"        bin/rails db:environment:set"ifdefined?(Rails.env)super("#{msg} RAILS_ENV=#{::Rails.env}\n\n")elsesuper("#{msg}\n\n")endend