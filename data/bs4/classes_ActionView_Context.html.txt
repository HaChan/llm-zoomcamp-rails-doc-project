Action ViewContextAction View contexts are supplied to Action Controller to render a template. The default Action View context isActionView::Base.In order to work with Action Controller, aContextmust just include this module. The initialization of the variables used by the context (@output_buffer, @view_flow, and @virtual_path) is responsibility of the object that includes this module (although you can call_prepare_contextdefined below).Methods#_layout_for,_prepare_contextAttributes[RW]output_buffer[RW]view_flowInstance Public methods_layout_for(name = nil)LinkEncapsulates the interaction with the view flow so it returns the correct buffer onyield. This is usually overwritten by helpers to add more behavior.Source:show|on GitHub# File actionview/lib/action_view/context.rb, line 27def_layout_for(name=nil)name||=:layoutview_flow.get(name).html_safeend_prepare_context()LinkPrepares the context by setting the appropriate instance variables.Source:show|on GitHub# File actionview/lib/action_view/context.rb, line 18def_prepare_context@view_flow=OutputFlow.new@output_buffer=ActionView::OutputBuffer.new@virtual_path=nilend